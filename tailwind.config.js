/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  // tailwind is just for utility spacing/flex — all visual style comes from classic.css
  theme: {
    extend: {},
  },
  plugins: [],
};
